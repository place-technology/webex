# `WebexWeb` keeps the controllers and views which
# define your web interface.
#
# `WebexWeb` is also responsible for housing the middleware
# and back-end logic related content.
module WebexWeb; end
